# 測試與品質查核報告（QA_REPORT）

研究基準日 2026-09-20。以下只列**實際執行過**的測試；沒有執行的項目列在最後一節。
重跑方式：`node tools/run_tests.js`、`python3 tools/test_lvr.py`、`node tools/ui_test.js`（結果分別寫入 `tools/test_results.json`、`tools/test_lvr_results.json`、`qa/ui_test_results.json` 與 `qa/*.png`）。

## 1. 自動化測試結果（最終版資料）

| 測試組 | 工具 | 結果 |
|---|---|---|
| 判斷邏輯＋正式資料檢查 | `tools/run_tests.js`（直接載入網站用的 `assets/logic.js`） | **54 通過／0 失敗** |
| 實價登錄解析器 | `tools/test_lvr.py` | **10 通過／0 失敗** |
| 介面（headless Chrome 以 `file://` 直接開啟 index.html，模擬操作） | `tools/ui_test.js` | **46 通過／0 失敗** |

測試用的假物件只存在於測試檔內（夾具），不會進入網站資料。

## 2. 你指定的 11 項驗收測試

| # | 要求 | 對應測試 | 結果 |
|---|---|---|---|
| 1 | 8,000 萬能通過；超過不能通過 | T1a（8,000 整 → 通過）、T1b（8,000.01 → 不通過）、T1c、T1d（超過預算不因可議價而放行） | 通過 |
| 2 | 房價＋必買車位超標不能誤判合格 | T2a（7,800＋必買 400＝8,200 → 超出預算）、T2b（可不買 → 以房價判斷）、T2c（是否必買不明且最壞情況超標 → 總價待確認）、T2d、T2e、T2f（總價已含車位 → 不重複加）、T8（同一戶含／不含車位兩種刊登 → 以屋價判斷） | 通過 |
| 3 | 主建物 25.99 坪不能因四捨五入通過 | T3a、T3b（25.9999）、T3c（顯示 25.999 → 「25.99」不進位）、T3d、T3e（23.4＋2.6 浮點相加 → 通過）、T3f | 通過 |
| 4 | 主 25＋附 4 只符合主＋附口徑 | T4a、T4b（標示文字「主＋附符合26坪；主建物未達26坪」）、T4c（A 模式不列入符合、B 模式列入） | 通過 |
| 5 | 缺關鍵資料不得顯示「已確認符合」 | T5a–T5h（缺主建物、只有估算值、缺開價、車位資訊不明、缺步行時間、登記疑義）、T7a／T7b（平台只給主＋附合計）、T9（住宅用途待確認）、D7（正式資料中「已確認符合」的每一間都有主建物 ≥26、總價 ok、步行時間）、D12（主建物＝總建坪未拆分者不會顯示已確認）、D13（登記用途含商業／辦公／住商者不會顯示已確認）、U3（總覽四類計數相加＝候選總數） | 通過 |
| 6 | 車位面積不會被重複扣除 | T6a–T6d（網站單價換算）、L1–L6（實價登錄：共有部分＝總−主−附−陽台−車位；①②③單價；車位價未拆分時不捏造） | 通過 |
| 7 | 重複刊登不會膨脹候選數 | D1（ID 唯一）、D2（同一刊登網址不會出現在兩個物件）、D3（樓層＋主建物＋總坪相同者已合併）、D11（樓層＋總坪＋土地持分＋開價相同者已合併或已標疑似） | 通過 |
| 8 | 收藏與筆記重新開啟後仍存在 | U12／U13（收藏、取消收藏）、U17（重新開啟後收藏仍在）、U18（筆記仍在）、U21（比較清單仍在）、U19（資料中已不存在的物件，其筆記仍保留並標示）、U20（JSON／CSV 匯出與匯入） | 通過 |
| 9 | 桌面及手機版沒有明顯版面或操作問題 | U31×9（390px 寬，九個頁面皆無水平溢出；以固定 390 為基準量測）、U32、`qa/desktop_*.png` 與 `qa/mobile_*.png` 人工檢視 | 通過（見 §5 已知限制） |
| 10 | 地圖載入失敗時其餘內容仍可閱讀 | U26（封鎖底圖網域 → 顯示提示，文字版位置清單完整）、U27、U28（模擬 Leaflet 元件載入失敗 → 顯示替代清單與 Google 地圖查詢連結）、U29、U30 | 通過 |
| 11 | 沒有示範資料、虛構連結、佔位內容 | D4（刊登網址皆為真實 http(s)）、D5（資料檔無 lorem／示範／placeholder／TODO）、D6／D15（沒有用 0 冒充缺值）、D16（網站資料沒有 591 網址或來源）、U33（頁面沒有 undefined／NaN／佔位字樣）、U34（全流程沒有 JavaScript 例外） | 通過 |

其他已測：口徑切換（U7）、總價／電梯篩選（U8、U9）、調整預算即時重算（U10、U35 逐字輸入不被打亂、U36 按 Enter 後套用）、表格模式（U11）、物件頁五個問題與原始連結（U14、U15）、並排比較 3 間（U16）、地圖標記與「直線距離圈」圖例（U22、U23）、圖表與樣本數（U24）、看屋清單與方法頁（U25）、預設排序單調（U37）、預設不隱藏候選（U38）、預售屋不與中古比價（D14）、無自相矛盾的車位資料（D10）。

## 3. 獨立品質查核（6 位查核員，唯讀）

定稿前以 6 個獨立 agent 從不同視角查核，只回報、不改檔；共約 85 項意見。以下是主要問題與處理：

| 嚴重度 | 問題 | 處理 |
|---|---|---|
| 高 | 新建案資料殘留 591 來源內容（第一次只刪掉含「591」字樣的句子，數值與座標仍在） | 改為欄位層級剔除；統計全部由官方預售實價登錄重算；唯一來源是 591 的建案不列入；原始檔移入隔離資料夾（D16 測試） |
| 高 | 至少 11 組同一戶未合併（各平台「主建物」口徑不同、樓層寫法不同、缺樓層） | 去重規則加入土地持分、主建物＝總坪、樓層正規化等證據；「疑似重複」機制原本沒有任何輸出，已修正。候選 291 → 280 |
| 高 | 主建物＝總建坪（未拆分）卻被判「已確認符合」 | <40 坪者列為面積待確認（D12 測試） |
| 高 | 登記用途含商業／辦公／住商、含地下層、地上權、隔套透天者混入「優先看屋」 | 任一平台用途取最不利者；地下層 → 待確認；地上權與隔套透天 → 不符合（D13 測試） |
| 高 | XA-007 車位另售卻顯示「總價在預算內」 | 改為總價待確認；加入一致性檢查（D10） |
| 高 | 規則式「同路段相近產品／路段統計」混入商業用、一樓店面、地上權、都更效益交易 | 只留住家用；一樓只與一樓比；新增備註旗標；同日同門牌多筆視為一個事件 |
| 高 | 規則式「推定同棟」抽查 12 間有 7 間混入他棟 | 有完整完工日者要求日期相符；跨多個完工日或巷者不當同棟；樣本 <3 不給百分比、不進評分；網站標示「規則式、未經查核」 |
| 高 | 預算／坪數輸入框打字中途重繪，數字被打亂 | 改為按 Enter 或離開欄位才套用（U35、U36） |
| 中 | 用 0 冒充缺值；小於 4 坪的「防空避難室兼停車場」被當成車位；東森只有主＋附合計；預售屋被標成新成屋並與中古比價；XA-144 單價用含車位價計算 | 全部修正（D14、D15、T7、T8） |
| 中 | 預設排序三個名單交錯；預設隱藏 23 間；手機兩頁水平溢出（原測試誤判通過）；列印會印出所有頁面；收藏後鍵盤焦點遺失；切換頁面不回頁首 | 全部修正；U31 改用固定 390px 量測，新增 U37、U38 |
| 中 | 文件互相矛盾（>15 分 vs >16.5 分、27 vs 26 間、面積待確認 32 vs 28、更新步驟三個版本、QA_REPORT 不存在） | 統一定義；洞察文字改為由資料計算；更新步驟以 README 為唯一版本 |
| 中 | 永慶／有巢氏資料取自頁面內嵌的編碼字串，且解碼座標曾用於初篩；截圖使用一般 Chrome UA | 在 METHODOLOGY §2 明確揭露並請你自行決定是否保留；截圖改用 headless 預設 UA、單一行程、被擋不重試 |
| 低 | 匯入備份未驗證格式、部分數字欄位未跳脫、hash 解碼例外、隱私模式未警告、圖表只靠顏色區分、地圖側欄不可鍵盤操作、文字對比不足 | 已修正 |

第一輪 6 間的價格敘述經成交查核員逐筆核對：引用的交易日、門牌、總價、主建物、②單價、百分比皆與實價登錄原始檔一致；實價登錄換算 810 筆獨立重算全部吻合。

## 4. 截圖涵蓋率

自動擷取的刊登頁截圖數量見網站總覽「有刊登頁截圖」卡（以及 `data/screenshots.json`）。第一輪 6 間與所有已深度查核的物件都有截圖。改用 headless 預設 User-Agent 後，中信房屋與東森房屋會回空白頁，依規範記為失敗、不重試；只在這兩個平台刊登的物件沒有截圖，請由原始連結開啟。

## 5. 沒有測試或無法測試的項目

- **Safari、Firefox、實體手機**：只用 Chrome（headless）測過，手機版是 390×844 模擬，沒有在實機上觸控操作。
- **深色模式**：有實作，但沒有自動化測試，只抽看過。
- **列印輸出**：修正了「列印時印出所有頁面」的樣式，但沒有實際輸出 PDF 檢查版面。
- **鍵盤全流程與螢幕閱讀器**：只修正了查核指出的問題，沒有做完整的無障礙測試；市場頁圖表點數多，鍵盤使用者請用各圖的「表格檢視」。
- **完全離線**：測了「底圖被封鎖」與「地圖元件載入失敗」兩種情況，沒有在真正斷網的環境下操作。
- **localStorage 被停用（隱私模式）**：有記憶體備援與警告訊息，但沒有自動化測試。
- **資料更新後的 ID 穩定性**：管線設計以 `data/id_registry.json` 保證，並在研究過程中多次重跑驗證 ID 不變；但沒有用「隔天重新擷取的新資料」做過端到端演練。
- **未查核物件的成交比對**：規則已收緊，但除了抽查之外沒有逐間人工驗證；網站上一律標示「規則式、未經逐筆查核」。
- **資料正確性的上限**：所有面積、屋齡、登記用途來自刊登頁，沒有任何一間以謄本核對；沒有實地踏勘；沒有向房仲確認可售狀態。
