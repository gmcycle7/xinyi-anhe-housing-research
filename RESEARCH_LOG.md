# 研究紀錄（RESEARCH_LOG）

研究基準日 2026-09-20（台北時間，UTC+8）。以下時間為當日實際執行時間。

## 時間軸

| 時間 | 事項 |
|---|---|
| 13:10 | 盤點工作目錄 `/Users/matthuang/claude_code/台北信義買房/`：空目錄，無其他專案檔案。建立獨立資料夾 `xinyi-anhe-housing-research/`（同名資料夾不存在，未覆寫任何檔案）。 |
| 13:11 | 測試各來源可否公開存取：信義、永慶、591、住商、台灣房屋、中信、實價登錄開放資料皆 HTTP 200；樂居、樂屋網 HTTP 403（未繞過）。讀取各站 robots.txt。 |
| 13:12 | 下載內政部實價登錄開放資料（臺北市買賣 113Q3–115Q2 共 8 季＋當期 115/8/21–8/31 登記；預售 114Q3–115Q2）。存於 `research_raw/lvr/`。 |
| 13:13 | 出入口：台北捷運官方車站頁為動態載入；以內建瀏覽器開啟官方頁面並讀取其官方 API 回應（SID=101）：出口 1、2、2A、3、4、5 共 6 個，含位置描述、座標、電梯。另以 OpenStreetMap 交叉核對（出入口編號與相對位置一致）。 |
| 13:15 | 寫入 `ASSUMPTIONS.md`（蒐集資料之前）。 |
| 13:18 | 啟動 9 個平行蒐集 agent：信義、永慶、591、住商、台灣房屋、中信、其他房仲（太平洋／東森／大家／21世紀／有巢氏）、預售新成屋、區域研究。 |
| 13:20–13:45 | 同步開發：實價登錄解析器（驗證「附屬建物」與「陽台」為獨立欄位、車位面積只扣一次）、判斷邏輯 `assets/logic.js` 與單元測試（當時 34 項，最終數量見 QA_REPORT）、網站骨架、Leaflet 本機化。 |
| 13:38 | 步行路由：python 內建 HTTPS 缺 CA 憑證 → 改用系統 curl（未關閉憑證驗證）。逐點請求遭路由服務限速（HTTP 429）→ 改為一次請求多起點的批次查詢（約 12 次請求完成全部），並加入退避。 |
| 13:50 | 第一次合併：發現中信房屋地址用阿拉伯數字段號（安和路1段）導致漏合併 → 加入地址正規化；重寫去重規則（見 METHODOLOGY §4）。 |
| 14:00 | **591 條款問題**：591 agent 回報物件頁頁尾聲明「禁止未經授權以爬蟲或自動下載程式擷取，違者每筆 3,000 元」。主研究員以存檔頁面查證屬實後：①立即停止進行中的深度查核與截圖工作（其中含 591 網址）；②將 `research_raw/591/` 移至 `_quarantine_591_not_used/` 並自資料管線移除；③重建資料，591 資料未進入任何結果。發現前 agent 已對 591 發出約 345 次請求（未登入、未繞過驗證）。其他平台的存檔頁面未見類似聲明。 |
| 14:02 | 永慶、有巢氏的座標只存在於頁面內嵌、經編碼的狀態字串 → 為避免有繞過平台防護之嫌，決定不使用其座標；只有路段者不填步行時間（位置待確認）。 |
| 14:05 | 物件 ID 定案（`data/id_registry.json`）。選出 27 間入圍物件（三類產品分層＋先補資料名單前段），啟動 9 個深度查核 agent；同時開始自動截圖。 |
| 14:16 | 地圖底圖：CARTO 圖磚出現「API KEY REQUIRED」浮水印；tile.openstreetmap.org 對無 Referer 的 curl 回 403，但瀏覽器以 file:// 開啟可正常載入。改用內政部國土測繪中心「臺灣通用電子地圖」為預設底圖、OpenStreetMap 為備用。 |
| 14:20 | 深度查核完成（27 間；每間核對原始頁、當日重抓、判讀文案與格局圖標註、找同門牌／同建案成交）。重要發現：多數入圍物件格局圖標註陽台外移；數間「推定同棟」比對混入鄰棟或一樓店面而方向相反（已由查核結果更正）；一間疑似已於 2026-01 成交；同一戶在同平台有兩種銷售組合（人工合併）。 |
| 14:25 | 預售新成屋 agent 曾讀取 591 新建案頁與 591 實價社區頁（約 35 次請求）。第一次處理只刪掉含「591」字樣的句子，品質查核指出數值與座標仍殘留 → 第二次改為**欄位層級剔除**：591 來源的座標、步行估算、公設比、開價、銷售動態全部捨棄；唯一來源是 591 且無實價登錄成交的建案不列入；統計全部由官方預售實價登錄逐筆重算（`tools/sanitize_newbuild_once.py`、`tools/build_newbuild.py`）；含 591 內容的原始檔移入隔離資料夾。 |
| 14:30 | 研究員定案第一輪 6 間與排序理由（`data/curation.json`）。開始擷取其餘候選的刊登頁截圖。 |
| 14:40–17:30 | 其餘候選的刊登頁截圖（背景擷取）。 |
| 17:00–18:30 | 品質查核 workflow：6 位獨立查核員（資料抽樣、去重、成交比對、程式碼、需求符合度、文件一致性）共提出約 85 項意見。主要修正：漏合併的同一戶（各平台主建物口徑不同、樓層寫法不同）→ 候選由 291 降為 280；主建物＝總建坪或含地下層 → 面積待確認；登記用途取各平台最不利者；地上權與隔套透天 → 不符合；XA-007 車位另售 → 總價待確認；規則式成交比對排除非住家用與一樓、樣本 <3 不給百分比；新建案 591 內容欄位層級剔除；預算輸入框重繪問題、排序、手機溢出、列印、鍵盤操作等介面修正。詳見 `QA_REPORT.md`。XA-239 已併入 XA-144（同一戶的兩種銷售組合）。 |
| 發布 | 使用者要求上傳 GitHub 並做成可分享的網站。事前告知：免費帳號的 GitHub Pages 需公開 repo（預算與看屋名單會公開）、截圖含平台著作與經紀人聯絡資訊。使用者選擇「公開 repo＋GitHub Pages」且「連截圖一起上傳」。`research_raw/`（原始頁面、隔離的 591 資料）與 `qa/*.png` 未上傳。 |

## 各平台蒐集結果（初篩保留數／掃描數）

| 平台 | 掃描 | 初篩保留 | 採用 |
|---|---:|---:|---|
| 信義房屋 | 959 | 171 | ✔ |
| 永慶房屋 | 387 | 110 | ✔（座標未使用） |
| 住商不動產 | 1,419 | 117 | ✔ |
| 中信房屋 | 855 | 71 | ✔ |
| 台灣房屋 | 114 | 9 | ✔ |
| 有巢氏房屋 | 124 | 22 | ✔（座標未使用） |
| 東森房屋 | 112 | 12 | ✔（無主建物單獨數字） |
| 太平洋房屋 | 76 | 8 | ✔（瀏覽器畫面轉錄） |
| 21世紀不動產 | 73 | 8 | ✔ |
| 大家房屋 | — | 0 | ✘ robots.txt 禁止的路徑才取得到 |
| 591 | 3,418 | 164 | ✘ 條款禁止 → 隔離未採用 |
| 樂屋網、樂居 | — | — | ✘ HTTP 403 |

各平台的搜尋網址、翻頁數、排除原因統計與限制，見 `research_raw/<平台>/ACCESS_NOTES.md`。

## 合併與篩選漏斗

初篩保留刊登 528 筆 → 去除非住宅類型後 491 筆 → 跨平台去重後 285 戶（含 1 組人工合併；品質查核前為 296 戶）→ 去除步行估算 >16.5 分者 5 戶 → **候選 280 戶**。最新數字以 `research_summary.md` 為準。

## 取得限制與處理方式（摘要）

1. 門牌：所有房仲平台只公開到路段（少數到巷）。→ 位置一律標為概略；步行時間為估算；深度查核時以實價登錄面積特徵反推門牌者，明確標示為「推定」。
2. 實價登錄沒有社區名。→ 以門牌或「路段＋總樓層＋完工年月」比對，標示為推定同棟。
3. 車位：多數平台不揭露車位單價、是否必買、能否分售。→ 依 ASSUMPTIONS §2 判為「總價待確認」或以最壞情況檢查。
4. 登記用途：少數刊登未揭露（合併後的間數見網站總覽與 research_summary）；另有一批登記用途含商業、辦公、住商等非純住家項目。→ 列為每間的待確認事項；明確非住家用者降為「住宅用途待確認」。
5. 照片：未逐張判讀（深度查核的物件僅判讀格局圖標註與少量照片）。
6. 未實地踏勘、未聯絡房仲、未送出表單、未預約、未對外發布。
