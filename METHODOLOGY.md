# 研究方法（METHODOLOGY）

研究基準日：2026-09-20。所有「今天」皆指此日（台北時間）。假設與口徑見 `ASSUMPTIONS.md`；逐步紀錄見 `RESEARCH_LOG.md`；測試見 `QA_REPORT.md`。

## 1. 流程總覽

```
口徑定義 (ASSUMPTIONS.md)
   ↓
官方出入口 (台北捷運官方 API) ─────────────┐
   ↓                                        │
各平台公開刊登頁蒐集（每平台一個 agent）     │
   ↓  research_raw/<平台>/listings.json      │
tools/build_dataset.py  合併 → 去重 → 定位 → 步行路由（OSM）
   ↓  research_raw/_merged_stage1.json、data/id_registry.json
tools/build_analysis.py  實價登錄比對 → 規則式分析 → 分級
   ↓  data/properties.json、data/comparables.json
深度查核（入圍 27 間，逐間；其中 1 間查核後確認與另一間是同一戶而合併 → 26 間）→ research_raw/verify/<ID>.json
   ↓  tools/apply_verification.py → data/analysis_overrides.json（＋ data/curation.json）
tools/capture_screenshots.py → screenshots/、data/screenshots.json
tools/build_bundle.js → data/bundle.js、exports/*.csv
   ↓
index.html（file:// 直接開啟）
```

## 2. 資料來源與用途

| 來源 | 用途 | 不代表 |
|---|---|---|
| 房仲刊登頁（信義、永慶、住商、台灣房屋、中信、太平洋、東森、21世紀、有巢氏） | 研究日的刊登開價、面積拆分、物件描述 | 不代表仍可售；面積未經謄本核實 |
| 內政部實價登錄開放資料（臺北市買賣，113Q3–115Q2 共 8 季＋115/9/11 期） | 歷史成交、面積拆分、車位拆分 | 不代表現在有同條件房屋出售 |
| 台北捷運官方車站資訊（SID=101） | 出入口編號、位置描述、座標、電梯 | — |
| OpenStreetMap（routing.openstreetmap.de foot profile、Nominatim） | 步行路徑距離估算、路段概略定位 | 不是實走結果；不含等紅燈 |
| 臺北市都更處、建管處、地政局、法規資料庫等（區域研究） | 都更／危老階段、行情指數、稅費規則 | 無法對應到特定在售物件 |

**未採用的來源**：
- **591**：蒐集後在其物件頁頁尾讀到「禁止未經授權以爬蟲或自動下載程式擷取，違者每筆 3,000 元」的聲明。發現後立即停止所有自動化存取，591 資料**完全未進入**網站與分析，原始檔隔離在 `research_raw/_quarantine_591_not_used/`（未擅自刪除，由你決定）。要看 591 請用一般瀏覽器人工瀏覽。
- 樂屋網、樂居：對一般請求回 HTTP 403，未嘗試繞過。
- 大家房屋：物件列表只能經 robots.txt 禁止的路徑取得，未抓取。
- 台灣房屋：robots.txt 禁止 `/sale/`、`/api/` 等；物件頁 `/buy/<編號>` 不在禁止範圍，僅抓允許路徑。
- 永慶、有巢氏：這兩個平台的物件資料放在頁面內嵌、經簡單編碼（base64 後字元位移）的狀態字串裡。**請你知悉並自行判斷是否保留這兩個平台**：蒐集 agent 在本機解碼了這個字串（沒有多發請求、沒有登入），價格與面積並以頁面可見文字交叉核對；永慶的解碼座標曾被用來做「直線 1,050 公尺」初篩。主研究員事後決定**不在研究結果中使用其座標**（這些物件若沒有其他平台座標，位置列為「待確認」），但初篩階段已受其影響。若你認為不妥，可刪除 `research_raw/yungching/` 與 `research_raw/others/` 中的有巢氏資料後重跑管線。

截圖：`tools/capture_screenshots.py` 以 headless Chrome 的預設 User-Agent（不偽裝成一般瀏覽器）、單一行程、每頁間隔 ≥2 秒開啟刊登頁；被阻擋或空白就記為失敗，不重試。（研究當天較早的一批截圖曾用一般 Chrome UA 並短暫以兩個行程擷取，經品質查核指出後已改正。）

存取規範：只開公開頁面、不登入、不繞過驗證碼或付費牆、遵守 robots.txt、單執行緒、請求間隔 ≥1.5 秒、未聯絡任何房仲、未送出任何表單。

## 3. 初篩（蒐集階段，寧可多留）

1. 位置：平台座標與車站中心直線距離 ≤1,050 公尺（**只用於初篩，從不當作步行距離**）；無座標者以路段白名單保留。
2. 價格：刊登總價 ≤8,800 萬（>8,000 萬者進「超出預算」名單）。
3. 面積：主建物 ≥24 坪，或主＋附 ≥26 坪，或無拆分但（總坪−車位）≥36 坪。
4. 類型：住宅；排除店面、辦公、純車位、土地、持分、廠辦、地下室。登記用途非住家者保留並標記。

## 4. 去重（`tools/build_dataset.py: same_unit`）

同一戶的必要條件：樓層與總樓層相同。再依證據強度：

| 判定 | 條件 |
|---|---|
| 同一戶（合併） | 主建物相同（±0.021 坪）且總坪相同（±0.06），並且同路段／同社區／座標 60 公尺內，或開價相同 |
| 同一戶（合併） | 主建物相同＋開價相同＋同路段（總坪差異推定為車位口徑） |
| 同一戶（合併） | 主建物相同＋總坪差額等於車位坪數＋同路段 |
| 同一戶（合併） | 一方缺主建物，但總坪相同、開價相近（±3%）、同路段 |
| 同一戶（合併） | 主建物差 ≤0.1、總坪差 ≤0.2、開價差 ≤3%、同路段（平台登載小數差異） |
| 同一戶（合併） | 總坪相同、開價相近、同路段，且（土地持分相同，或其中一個平台的主建物＝總坪）——主建物數字不同是因為平台未拆分或把陽台／地下室併入 |
| 同一戶（合併） | 一個平台沒有樓層，但總坪、土地持分、開價完全相同且位置吻合 |
| 疑似重複（不合併，互相標示） | 同樓層且主建物相同但總坪與開價都不同；總坪或土地持分相同但主建物不同；樓層不同但其餘全部相同（可能是樓層登錄錯誤）且兩邊沒有共同平台 |
| 不同戶（規則判定，未經人工複核） | 同一直營平台以不同編號、不同價格刊登。此規則有例外：XA-144 即是同平台以兩個編號刊登同一戶的兩種銷售組合，經查核後以 `data/manual_merge.json` 人工合併 |

樓層比對前先正規化（「11樓」「11F」→ 11；「7-8／7」這類頂樓＋加蓋層以登記樓層比對）。平台以 0 或「--」表示的面積一律視為缺值。

合併後保留**每一個平台的網址、物件編號、開價、查閱時間**；開價不同時全部並列，預算判斷用最高開價做保守判斷（最低 ≤ 預算 < 最高 → 「總價待確認」）。各平台主建物數字不同時取較小者；若跨過 26 坪門檻則列為待核對。各平台屋齡差 >3 年時取中位數並提示。

物件 ID（`XA-###`）記錄在 `data/id_registry.json`（鍵＝`平台:物件編號`），重跑不會改變既有 ID。

## 5. 位置與步行時間

- 終點：官方 6 個可進站出入口（1、2、2A、3、4、5）。
- 起點：刊登頁提供的物件座標。平台不公開門牌，座標可能偏移 → 一律標「概略位置」。同一戶有多個平台座標時各算一次，取中位數，並顯示範圍。
- 路徑：OSM 步行路網（OSRM table，一次請求多個起點以降低對公開服務的負擔），取最近出入口；起點吸附到路網的距離也計入。
- 時間＝距離 ÷ 80 公尺/分，進位到 0.5 分；不含等紅燈。
- 9–11 分、14–16 分，或各平台估算跨過 10／15 分者標「邊界，需實走」。
- 只有路段、沒有座標者：**不填步行時間**（位置待確認），僅顯示「路段代表點粗估」供參考，不參與符合判斷。
- >16.5 分者不列入候選（紀錄於 `research_raw/_out_of_range.json`）。

## 6. 預算與面積判斷（`assets/logic.js`，網站與測試共用同一份程式）

- 預算：見 ASSUMPTIONS §2。三種結果：在預算內／超出預算／總價待確認。
- 面積：A＝主建物 ≥26；B＝主＋附 ≥26。以 1/10000 坪整數比較，不四捨五入；顯示時無條件捨去到小數 2 位。
- 登記疑義（`area.doubt`）：無電梯公寓、刊登頁沒有共有部分、且主建物在 26–30 坪之間 → 樓梯間可能已計入主建物，扣除後可能低於 26 坪 → 列為「面積待確認」。（30 坪以上者即使扣除一般樓梯間持分仍高於門檻，只提示不降級。這是研究假設。）
- 任一平台的登記用途含非純住家項目（事務所、商業、辦公、住商、店鋪、其他、防空避難室、儲藏室），或本戶含地下層 →「住宅用途待確認」，不列入已確認符合；多平台用途不一致時取最不利者並全部列出。地上權／租賃權產品、隔套收租透天 → 不符合（非一般完整住宅）。刊登頁完全未揭露登記用途者不因此降級，但在物件頁列為待確認事項。
- 主建物＝總建坪且 <40 坪、或含地下層 → 面積待確認。小於 4 坪且沒有任何車位說明的「防空避難室兼停車場」持分不視為車位。

## 7. 成交比對

實價登錄不含社區名，比對方式：

1. **深度查核的物件**：有門牌或巷弄時以「同路＋同巷＋同號」找同門牌成交，再以「同巷＋總樓層＋建築完成年月」擴大到同社區。逐筆人工檢視。
2. **其餘物件（規則式，未經逐筆查核）**：同路段＋總樓層相同＋建築完成日（刊登頁有完整完工日者要求相差 ≤31 天；只有屋齡者容許 ±0.8 年）。比對結果若跨多個完工日或多個巷，代表只是「同路段同期興建的建物」→ 不當成同棟、不計統計。品質查核曾抽查 12 間發現 7 間混入他棟，因此這類比對在網站上一律標示「規則式、未經查核」，只當線索。
3. 鄰近相似產品：同路段、**主要用途為住家用**、同電梯／無電梯類別、**一樓只和一樓比、樓上只和樓上比**、屋齡差 ≤10 年、主建物為本戶的 60–140%、近 24 個月、無特殊備註、車位可拆分；同日同門牌多筆視為一個事件；最多 10 筆。
4. 預售屋不與中古成交比較。

期間：優先 24 個月；同棟樣本 <2 筆時延伸到 36 個月並標示。
排除於統計之外（但仍列出並標記）：特殊關係交易、含增建、持分、急售／債務、瑕疵、政府標售、合建／地主戶、多層或含地下層移轉、含租約、地上權／承租土地、具重建或都更效益、預售、車位無法拆分、車位面積口徑異常（車位面積大於「總−主−附−陽台」）。

三種單價：①（總價−車位價）÷（總坪−車位坪）；②（總價−車位價）÷ 主建物；③（總價−車位價）÷（主建物＋附屬建物＋陽台）。實價登錄的「附屬建物面積」與「陽台面積」是兩個欄位，B 口徑的「附」＝兩者相加；共有部分＝總面積−主−附−陽台−車位，車位面積只扣一次（已用資料驗證並有單元測試）。

樣本 ≥3 筆才給中位數、範圍與「開價高於／低於中位數」的百分比，也才會進入評分與缺點；不足 3 筆只逐筆列出各自的差距。只陳述「開價高於／低於可比成交多少」，不推論可議價幅度。

## 8. 分級與排序

- **目前不符合條件**：超出預算、或兩種面積口徑皆未達、或步行 >15 分。
- **先補資料再決定**：總價／面積／位置／住宅用途任一待確認；或只符合 B 口徑；或深度查核後建議先補資料。
- **優先看屋**：依刊登頁數字三項皆符合。其中「第一輪」為逐間深度查核後由研究員選定的 5–10 間；其餘為「第二輪候補」（規則式初判，未逐間查核）。

規則式評分（只用於同一名單內排序與挑選查核對象；公開如下）：
步行（15−分鐘）×2 ＋ 主建物超出 26 坪的坪數（上限 10）＋ 開價相對可比成交的折溢價（±15% 內，同棟權重 0.6、鄰近 0.3）＋ 有電梯 4 ＋（45−屋齡）×0.25 ＋ 預算餘裕（(8000−開價)/8000×10）− 頂樓或一樓 3 − 提及增建 4 − 位置邊界 2。

入圍深度查核的方式：把符合者分成「電梯且屋齡 ≤30 年」「電梯且屋齡 >30 年」「無電梯」三類，各取分數最高的至多 5／13／5 間（第一類當時只有 3 間符合步行 ≤10 分，另補 2 間 10–15 分者），再加「先補資料」名單中分數最高的 6 間，共 27 間。分三類是因為你尚未指定屋齡、電梯偏好，不應讓單一評分把某一類產品全部擠掉。

第一輪名單的最終排序由研究員依查核結果決定，理由寫在每間物件的「排序理由」。**不以最接近 8,000 萬為優、不以資料完整度為優。**

## 9. 截圖

`tools/capture_screenshots.py` 以 headless Chrome 開啟原始刊登頁存成 JPEG（保留頁面上的來源與浮水印；著作權屬各房仲平台）。原本設定為僅供本機個人研究；2026-09-20 使用者在被告知著作權與個資疑慮後，決定連同截圖一起公開發布。若日後要撤下，刪除 `screenshots/` 並執行 `python3 tools/capture_screenshots.py --index-only`、`python3 tools/build_analysis.py`、`node tools/build_bundle.js` 後重新推送即可。網站的卡片縮圖與物件頁「刊登頁截圖」皆來自這些檔案，並標示平台與擷取時間。未使用任何生成的房屋照片或格局圖。

## 10. 如何更新房源（不破壞收藏與筆記）

完整指令以 `README.md`「更新房源」一節為唯一版本（本節不重複列出，避免版本不一致）。重點：

1. **不要刪除 `data/id_registry.json`**：它保證同一戶的 ID 不變。收藏與筆記存在瀏覽器 localStorage（鍵 `xinyiAnhe.userdata.v1`），以 ID 為鍵，和 `data/` 完全分開；下架的物件其筆記仍會顯示在「收藏與筆記」頁。更新前建議先在該頁匯出 JSON 備份。
